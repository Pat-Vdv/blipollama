[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "blipollama"
version = "0.1.0"
description = "BLIP/Ollama captioning wrapper"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["blipollama"]

